var words = ['spray', 'limit', 'exuberant', 'destruction', 'elite', 'present'];

words.forEach((index) => {
    if (index[0] == 'a' || index[0] == 'e' || index[0] == 'i' || index[0] == 'o' || index[0] == 'u')
    {
        console.log(index);
    }
})